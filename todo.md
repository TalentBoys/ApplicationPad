全部做完后总结：

文件夹相关的 todo:
请一个个完成，并打勾。




issue:
请一个个修复，并打勾。



看代码修复：
3. [x] 把 app 拖动到一个已经存在的 folder 的时候，如果在 folder merge 的范围内停留 2s 不松开鼠标，则打开 folder，这时用户可以把 app 拖动到 folder 里面的任意某个位置。 没有打开文件夹。
1. [x] 全屏打开 launcher 的时候希望有一个展开动画。关闭有隐去动画。有一点延迟的感觉。
3. [x] folder 中的 app 应该支持拖动到 folder 外，如果往 folder 外拖动，则 folder 收起，展示外部 launcher。到了外部以后，逻辑就和在 launcher 里拖动一样。

换位到一些没有 app 存在的地方也是允许的。直接占据那个空位。（看一下他换位逻辑的代码，index 怎么交换）



现在判定还有点问题。特别是拖拽到右侧，需要仔细看看。最好能写 UT。让他把判定逻辑抽象出来，先不和 UI 位置强相关。位置的逻辑是一个函数。知道位置以后，做的决定是另一个函数。


